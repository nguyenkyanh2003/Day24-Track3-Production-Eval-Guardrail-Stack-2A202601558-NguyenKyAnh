# Choose one provider. Gemini is preferred when both keys are present.
GEMINI_API_KEY=
OPENAI_API_KEY=

# Optional overrides (defaults are provider-aware).
LLM_MODEL=
LLM_MAX_RPM=14

# Optional: authentication for gated Hugging Face model downloads.
HF_TOKEN=
